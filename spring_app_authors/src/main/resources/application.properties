spring.application.name=${SPRING_APPLICATION_NAME:spring_app_authors}

# String de conexión a la base de datos
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/distribuida}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:rc2ijxxz}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

server.port=${SERVER_PORT:1010}

# Consul
consul.host=${CONSUL_HOST:127.0.0.1}
consul.port=${CONSUL_PORT:8500}
